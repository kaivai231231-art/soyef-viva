[build]
  base = "artifacts/viva-study"
  command = "pnpm install && pnpm build && APP_UPDATE_VERSION=${APP_UPDATE_VERSION:-1.0.1} bash scripts/generate-app-update.sh"
  publish = "dist/public"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
